# read lemgth
length=float(input("entre the length in metres: "))
# read width
width=float(input("entre the width in metres: "))

# calculate area l*w
area=length*width
# print area
print(f"total area of the room: {area} mt^2")